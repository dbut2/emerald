// Code generated by cmd/recomp from pokeemerald_modern.elf. DO NOT EDIT.
package emerald

import g "dbut.dev/sapphire/gba"

type reg struct {
	addr, size uint32
	fn         func(*g.CPU)
}

func Install() {
	for _, r := range regs0 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs1 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs2 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs3 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs4 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs5 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs6 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs7 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs8 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs9 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs10 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs11 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs12 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs13 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs14 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs15 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs16 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs17 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs18 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs19 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs20 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs21 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs22 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs23 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs24 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs25 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs26 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs27 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs28 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs29 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs30 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs31 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs32 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs33 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs34 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs35 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs36 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs37 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs38 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs39 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
	for _, r := range regs40 {
		g.RegisterNative(r.addr, r.size, r.fn)
	}
}

func Entry(c *g.CPU) { f_08000000(c) }
